name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code on pi5
        run: |
          cd ~/home-assistant/pi-dumb2smart-ir && git pull
